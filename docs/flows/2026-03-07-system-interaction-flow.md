# სისტემური ურთიერთქმედების Flow

> როგორ ურთიერთქმედებს სისტემის სხვადასხვა ნაწილი ერთმანეთთან

---

## მთავარი ურთიერთქმედების დიაგრამა

```
┌─────────────┐                                    ┌─────────────┐
│   CARRIER   │                                    │   DRIVER    │
│  DISPATCHER │                                    │  (CDL only) │
└──────┬──────┘                                    └──────┬──────┘
       │                                                  │
       │  1. რეგისტრაცია                                  │
       │─────────────────▶┌──────────────────┐            │
       │                  │                  │            │
       │  2. Login        │   REPORT MARKET  │            │
       │─────────────────▶│                  │            │
       │                  │   ┌──────────┐   │            │
       │  3. ძებნა CDL    │   │          │   │  7. CDL    │
       │─────────────────▶│   │ DATABASE │   │◀───────────│
       │                  │   │          │   │            │
       │◀─────────────────│   │ Users    │   │  8. რეიტინგი│
       │  4. პროფილი +    │   │ Drivers  │   │───────────▶│
       │     რეიტინგი     │   │ Reports  │   │            │
       │                  │   │          │   │  9. Dispute │
       │  5. რეპორტი      │   └──────────┘   │◀───────────│
       │─────────────────▶│                  │  (48სთ)    │
       │                  │  ┌────────────┐  │            │
       │  6. დადასტურება  │  │ Rating     │  │ 10. ახალი  │
       │◀─────────────────│  │ Engine     │  │  რეიტინგი  │
       │                  │  │            │  │───────────▶│
       │                  │  │ 100 - Σw   │  │            │
       │                  │  └────────────┘  │            │
       │                  │                  │            │
       │                  └──────────────────┘            │
       │                                                  │
```

---

## რეპორტის შექმნის Sequence

```
Carrier/Dispatcher          Server                    Database
       │                      │                          │
       │  POST /report        │                          │
       │  {cdl, category,     │                          │
       │   subcategory,       │                          │
       │   description, date} │                          │
       │─────────────────────▶│                          │
       │                      │                          │
       │                      │  1. Validate (Zod)       │
       │                      │─────────┐                │
       │                      │◀────────┘                │
       │                      │                          │
       │                      │  2. Check auth (JWT)     │
       │                      │─────────┐                │
       │                      │◀────────┘                │
       │                      │                          │
       │                      │  3. findOrCreate Driver  │
       │                      │─────────────────────────▶│
       │                      │     SELECT * FROM drivers│
       │                      │     WHERE cdl = ?        │
       │                      │◀─────────────────────────│
       │                      │                          │
       │                      │  (თუ არ არსებობს)        │
       │                      │  4. CREATE Driver        │
       │                      │─────────────────────────▶│
       │                      │     INSERT INTO drivers  │
       │                      │◀─────────────────────────│
       │                      │                          │
       │                      │  5. CREATE Report        │
       │                      │─────────────────────────▶│
       │                      │     INSERT INTO reports  │
       │                      │     status: VERIFIED     │
       │                      │◀─────────────────────────│
       │                      │                          │
       │                      │  6. revalidatePath       │
       │                      │─────────┐                │
       │                      │◀────────┘                │
       │                      │                          │
       │  ✅ Success          │                          │
       │◀─────────────────────│                          │
       │                      │                          │
```

---

## რეიტინგის მოთხოვნის Sequence

```
  მძღოლი (ან Carrier)         Server                    Database
       │                         │                          │
       │  GET /drivers/[cdl]     │                          │
       │────────────────────────▶│                          │
       │                         │                          │
       │                         │  1. Find Driver by CDL   │
       │                         │─────────────────────────▶│
       │                         │     SELECT * FROM drivers│
       │                         │     WHERE cdl = ?        │
       │                         │◀─────────────────────────│
       │                         │                          │
       │                         │  (თუ არ არსებობს)        │
       │  "სუფთა ჩანაწერი 100%" │                          │
       │◀────────────────────────│                          │
       │                         │                          │
       │                         │  (თუ არსებობს)           │
       │                         │  2. Get all reports      │
       │                         │─────────────────────────▶│
       │                         │     SELECT * FROM reports│
       │                         │     WHERE driverId = ?   │
       │                         │     ORDER BY createdAt   │
       │                         │◀─────────────────────────│
       │                         │                          │
       │                         │  3. Calculate rating     │
       │                         │─────────┐                │
       │                         │  filter: │                │
       │                         │  VERIFIED│                │
       │                         │  only    │                │
       │                         │         ▼                │
       │                         │  Σ weights                │
       │                         │  score = max(0, 100 - Σ) │
       │                         │  color = fn(score)       │
       │                         │◀────────┘                │
       │                         │                          │
       │                         │  4. Check session        │
       │                         │─────────┐                │
       │                         │◀────────┘                │
       │                         │                          │
       │  Profile + Rating       │                          │
       │  (აღწერა: auth-ზე      │                          │
       │   დამოკიდებული)        │                          │
       │◀────────────────────────│                          │
       │                         │                          │
```

---

## გასაჩივრების Sequence

```
  მძღოლი                      Server                    Database
       │                         │                          │
       │  CDL-ით შესვლა          │                          │
       │────────────────────────▶│                          │
       │                         │                          │
       │  პროფილის ნახვა         │                          │
       │◀────────────────────────│                          │
       │                         │                          │
       │  (ხედავს რეპორტს       │                          │
       │   < 48სთ)               │                          │
       │                         │                          │
       │  [გასაჩივრება]          │                          │
       │  {reportId, reason}     │                          │
       │────────────────────────▶│                          │
       │                         │                          │
       │                         │  1. Find Report          │
       │                         │─────────────────────────▶│
       │                         │◀─────────────────────────│
       │                         │                          │
       │                         │  2. Check 48hr window    │
       │                         │─────────┐                │
       │                         │  now - createdAt < 48hr? │
       │                         │◀────────┘                │
       │                         │                          │
       │                    ┌────┴────┐                     │
       │                    ▼         ▼                     │
       │               < 48სთ     > 48სთ                   │
       │                    │         │                     │
       │                    ▼         ▼                     │
       │              3. Update    ❌ Error                 │
       │              status       "ვადა გასულია"           │
       │                    │                               │
       │                    │─────────────────────────────▶│
       │                    │  UPDATE reports               │
       │                    │  SET status = 'DISPUTED'     │
       │                    │  WHERE id = ?                │
       │                    │◀─────────────────────────────│
       │                    │                               │
       │  ✅ გასაჩივრებულია │                               │
       │  ახალი რეიტინგი:   │                               │
       │  XX% → YY% (+Z)    │                               │
       │◀───────────────────│                               │
       │                    │                               │
```

---

## ავტორიზაციის ფენები

```
┌─────────────────────────────────────────────────────────┐
│                     MIDDLEWARE                            │
│  src/middleware.ts                                        │
│  matcher: ["/dashboard/:path*"]                          │
│                                                          │
│  ┌───────────────────────────────────────────────────┐   │
│  │ auth() → session?                                 │   │
│  │   ├─ არსებობს → გაატარე                          │   │
│  │   └─ არ არსებობს → redirect("/login")            │   │
│  └───────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│               DASHBOARD LAYOUT                           │
│  src/app/(dashboard)/layout.tsx                          │
│                                                          │
│  ┌───────────────────────────────────────────────────┐   │
│  │ const session = await auth();                     │   │
│  │ if (!session?.user) redirect("/login");           │   │
│  └───────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│               SERVER ACTIONS                             │
│  src/features/reports/actions.ts                         │
│                                                          │
│  ┌───────────────────────────────────────────────────┐   │
│  │ const session = await auth();                     │   │
│  │ if (!session?.user?.id)                           │   │
│  │   return { error: "Unauthorized" };               │   │
│  └───────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## გვერდების ხელმისაწვდომობის მატრიცა

```
                    Carrier/    მძღოლი    არავინ
                    Dispatcher  (CDL)     (Guest)
                    ──────────  ────────  ────────
Landing /            ✅          ✅        ✅
Login                ✅          ✅        ✅
Register             ✅          ✅        ✅
Dashboard            ✅          ❌        ❌
Report/new           ✅          ❌        ❌
Driver-check         ✅          ✅        ✅
Drivers/[cdl]        ✅*         ✅**      ✅**

* სრული ხედი (აღწერა ხილვადია)
** შეზღუდული ხედი (აღწერა დამალული)
```
